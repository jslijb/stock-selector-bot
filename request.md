以下是我们在前几轮讨论中逐步达成一致并最终确认的方案总结，未包含任何被否定或调整掉的内容：

---

## 项目定位
开发个人用、单机、零预算、无 GPU 的**认知型智能选股 Agent**，兼具量化因子、机器学习、大模型推理、情景记忆与自我进化能力，用于实战练手。

---

## 整体架构
- **单 Agent 模型**：一个核心程序，内部包含感知、混合推理、行动与风控、情景记忆、自我反思等模块。
- **运行方式**：人工每日手动运行（或 crontab 定时），若有遗漏可后续补跑，数据与决策自动补全。
- **交互界面**：CLI 或 Streamlit 轻量前端。

---

## 数据层方案
- **数据存储**：DuckDB（单文件、列式分析、高效查询）
- **数据源**：Tushare 免费额度，key 已创建好环境变量： TUSHARE_TOKEN
- **无需分布式系统**，普通笔记本完全满足。

---

## 感知模块
1. **量价/基本面因子**：构建 **150+ 个因子**（估值、质量、成长、动量/反转、资金/情绪、技术、另类事件等），每日批量计算。
   - 计算引擎：Python + DuckDB，通过函数注册所有因子，一次运行生成因子宽表。
   - 核心处理：行业中性化、市值中性化、去极值、Z-Score。
2. **NLP 舆情**：使用**云端 LLM API**（如 DeepSeek/GLM-4-Flash 免费额度）对新闻进行情感三分类和事件类型打标，结果结构化存储于 `news_tags` 表，作为数值情绪因子。成本接近零。
   - 不本地微调 BERT。
3. **另类数据**：北向资金、龙虎榜、融资融券等直接入库。

---

## 情景记忆系统
- 使用 **ChromaDB** 存储市场状态快照的 embedding 向量及描述。
- **嵌入模型**：`BAAI/bge-large-zh-v1.5`（效果优先，CPU 可承受）。
- 每日决策时检索 top-3 历史相似日，获取当时选股和后续表现，为 LLM 推理提供参考。

---

## 混合推理引擎（三层筛选）
1. **第一层 – 多因子打分**：基于当前因子权重模板对全市场计算综合得分，取前 20% 进入候选池。
2. **第二层 – XGBoost/LightGBM 精选**：对候选池二次打分，取前 50 只进入精选池；使用 SHAP 解释关键驱动因子。
3. **第三层 – LLM 认知决策**：输入精选池摘要、市场环境、历史相似记忆、SHAP 解释等，由云端 LLM 给出最终 20 只持仓及权重建议，并生成自然语言理由。

---

## 组合构建与风控
- 单票权重上限 10%
- 行业偏离不超过基准 ±5%
- LLM 输出最终权重，系统做简单硬约束校验

---

## 自我进化机制（月度离线进化）
1. **绩效归因**：
   - 记录每日决策及事后 N 日收益。
   - 每月计算各因子的滚动 IC（Rank IC）、因子间 IC 协方差矩阵。
2. **权重优化**：
   - 根据过去 3 个月 IC 均值与协方差，最大化 IR（信息比率）求解新一期因子权重。
   - 权重约束：单因子权重 [1%, 30%]，总和 100%。
3. **人工确认**：新权重推送至前端，经你审阅后副用，形成半自动进化闭环。

---

## 数据库完整 Schema
确认使用 DuckDB，并建立了以下 10 张核心表：
1. `daily_price` – 日线行情
2. `adj_factor` – 复权因子
3. `financials` – 财务数据（季频/年频）
4. `money_flow` – 资金流向（北向、融资融券等）
5. `news_tags` – LLM 打标后的新闻结构化标签
6. `factors_daily` – 日频因子宽表
7. `market_state_snapshot` – 市场状态快照与向量
8. `decisions` – 每日决策日志（股票、权重、理由）
9. `decision_performance` – 决策事后绩效跟踪
10. `factor_weights_history` – 因子权重进化记录

---

## LLM 选股 Prompt 结构
明确固定为包含以下要素的模板：
- 当前市场环境（文字描述 + 量化指标）
- 历史相似时期参考（检索到的 top-3 日及其经验）
- 精选候选池（含量化得分、ML 评分、主要因子、近一周事件、SHAP 驱动因子）
- 任务要求：挑选 20 只、给出权重与理由、输出 JSON

---

## 调度与容错
- 无需 Airflow，人工每日运行（或 crontab）
- 若错过某日，脚本支持自动检测缺失日期并批量补跑数据、因子和决策

---

## 环境准备说明，需要补充。